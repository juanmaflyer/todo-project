== Lista de Tareas ==

Esta aplicación de rails es una simple implementación de una lista de tareas.
Se puede realizar el registro de usuario, y cada usuario tiene la posibilidad de crear una o más listas de tareas a las cuales se les puede asignar, a su vez, varias tareas.

Para el sistema de autenticación y control de sesiones se utilizó Authlogic.


